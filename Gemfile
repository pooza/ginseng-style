source 'https://rubygems.org'
gemspec

# ⚠ このリポジトリの Rakefile を回すためだけに要る。他の gem は ginseng-core 経由で
# rake が入るが、⚠⚠ この gem は ginseng-core に依存させられない（循環する）。
# ⚠ 開発依存は gemspec ではなくここに書く（`Gemspec/DevelopmentDependencies`）。
gem 'rake', require: false
